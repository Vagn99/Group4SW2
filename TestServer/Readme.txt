To run the server, Node.js and node package manager (npm) must be installed on the system. 

To run the server you must:
	- Open a terminal in the TestServer folder
	- Run "npm install"
	- Run "npm run build"
	- The game can now be accessed on http://localhost:3000

You can login as user1, user2, user3, user4, user5, and user6.
The passwords are the same as the usernames.